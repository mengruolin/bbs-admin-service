

export default () => {
  // app.passport.verify(async (ctx: Context, user) => {
    
  //   ctx.logger.debug('passport.verify', user)
  //   const { username, password } = user.payload
  //   const existUser: any = await ctx.service.user.getUserByLoginName(username)

  //   if (!existUser || existUser.password !== password) return null
    
  //   ctx.payload = user.payload
  //   return user
  // })
}